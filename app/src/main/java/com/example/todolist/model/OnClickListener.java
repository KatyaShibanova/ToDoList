package com.example.todolist.model;

import com.google.android.material.textfield.TextInputEditText;

interface OnClickListener {
    void onClick(TextInputEditText name, TextInputEditText description);
}
